# delete me when js testing ist running well. until then I stay for testing
describe 'I am a blank test case for karma', ->
  a = undefined
  it 'and so is a spec', ->
    a = true
    expect(a).toBe true
    return
  return
